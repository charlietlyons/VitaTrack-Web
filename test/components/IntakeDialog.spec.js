describe("IntakeDialog", () => {
  it("should send addIntake request when submit is pressed", () => {});

  it("should setShowDialog to false when addIntake call successful", () => {});

  it("should setError to Something went wrong if addIntake call errors", () => {});

  it("should set food and quantity states when user inputs values", () => {});

  it("should show add food dialog if Add Food button pressed", () => {});

  it("should stop displaying when pressing Cancel", () => {});
});
